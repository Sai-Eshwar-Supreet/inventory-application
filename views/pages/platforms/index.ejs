<!DOCTYPE html>
<html lang="en">
    <%- include('../../partials/head') %>
    <body>
        <%- include('../../partials/header') %>

        <div class="page-header">
            <%- include('../../partials/headers/page-header', 
                {
                    heading: 'Platforms'
                }
            )%>
            <%- include('../../partials/actions/create-action', 
                {
                    action: '/platforms/new', 
                    label: 'New'
                }
            )%>
        </div>

        <main class="content">
            <ul class="list-card-container">
                <% platforms.forEach(platform => { %>
                    <li class="list-card">
                        <%- include('../../partials/headers/card-header', 
                            {
                                heading: platform.name
                            }
                        )%>

                        <div class="tool-bar">
                            <%- include('../../partials/actions/edit-action', 
                                { 
                                    action:`/platforms/${platform.id}/edit` 
                                }
                            )%>
                            <%- include('../../partials/actions/delete-action', 
                                { 
                                    action:`/platforms/${platform.id}/delete`, 
                                    label: "Delete" 
                                }
                            )%>
                        </div>
                    </li>
                <% }) %>
            </ul>
        </main>

        <%- include('../../partials/footer') %>
    </body>
</html>